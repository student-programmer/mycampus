'use client'

import { Connects } from '@/fsd/pages/Connects';
import BaseLayout from "@/fsd/app/BaseLayout";

export default function Page() {
    return < BaseLayout Component={ Connects } navMenuOn/>;
}
